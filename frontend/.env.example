# Base URL of the backend API.
# Dev (vite dev server talking to uvicorn directly):
VITE_API_URL=http://localhost:8000
# Production (served behind nginx, API proxied under /api): set VITE_API_URL=/api
