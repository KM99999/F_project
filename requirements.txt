fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt 4.0.1: passlib 1.7.4 reads bcrypt.__about__.__version__, which newer
# bcrypt (>=4.1) removed — causing a noisy "error reading bcrypt version" log.
bcrypt==4.0.1
python-multipart==0.0.20

# AI / document processing (Fase 2 — pipeline de extracción).
# Floors (not exact pins) so the first build resolves a current working version.
anthropic>=0.40
pdfplumber>=0.11
pillow>=10.0
# Detección visual (Fase 3 — hash perceptual / pHash)
imagehash>=4.3
